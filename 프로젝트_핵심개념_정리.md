# Spring Blog 프로젝트 핵심 개념 정리

## 📋 목차
1. [프로젝트 개요](#프로젝트-개요)
2. [아키텍처 구조](#아키텍처-구조)
3. [핵심 개념](#핵심-개념)
4. [주요 기능별 설명](#주요-기능별-설명)
5. [설정 파일 설명](#설정-파일-설명)
6. [주요 변경사항](#주요-변경사항)

---

## 프로젝트 개요

### 기술 스택
- **프레임워크**: Spring Boot 3.4.12
- **언어**: Java 17
- **빌드 도구**: Gradle
- **데이터베이스**: H2 (인메모리, MySQL 호환 모드)
- **ORM**: Spring Data JPA (Hibernate)
- **템플릿 엔진**: Mustache (SSR - Server Side Rendering)
- **인증 방식**: 세션 기반 인증

### 프로젝트 구조
```
src/main/java/org/example/demo_ssr_v1_1/
├── _core/                    # 공통 설정 및 유틸리티
│   ├── config/              # 설정 클래스
│   ├── interceptor/         # 인터셉터
│   └── errors/              # 예외 처리
├── board/                   # 게시글 도메인
│   ├── Board.java          # 게시글 엔티티
│   ├── BoardController.java
│   ├── BoardService.java
│   ├── BoardRepository.java
│   └── BoardRequest.java   # DTO
├── user/                    # 사용자 도메인
│   ├── User.java           # 사용자 엔티티
│   ├── UserController.java
│   ├── UserService.java
│   ├── UserRepository.java
│   └── UserRequest.java    # DTO
└── utils/                   # 유틸리티 클래스
```

---

## 아키텍처 구조

### 3-Tier Architecture (3계층 아키텍처)

```
┌─────────────────────────────────────┐
│   Controller (표현 계층)            │
│   - HTTP 요청/응답 처리              │
│   - 세션 관리                        │
│   - View 이름 반환                   │
└──────────────┬──────────────────────┘
               │ 요청 위임
               ↓
┌─────────────────────────────────────┐
│   Service (비즈니스 계층)           │
│   - 비즈니스 로직 처리               │
│   - 트랜잭션 관리 (@Transactional)   │
│   - 여러 Repository 조합             │
└──────────────┬──────────────────────┘
               │ 데이터 접근
               ↓
┌─────────────────────────────────────┐
│   Repository (데이터 접근 계층)      │
│   - JPA 쿼리 메서드                  │
│   - 데이터베이스 CRUD 작업            │
└──────────────┬──────────────────────┘
               │
               ↓
┌─────────────────────────────────────┐
│   Database (H2)                     │
└─────────────────────────────────────┘
```

### 계층별 책임 분리

#### 1. Controller (표현 계층)
- **역할**: HTTP 요청을 받아서 처리하고 응답을 반환
- **책임**:
  - 요청 데이터 검증 및 파라미터 바인딩
  - 세션 관리 (로그인 사용자 정보)
  - Service 레이어에 비즈니스 로직 위임
  - View에 데이터 전달 또는 리다이렉트

#### 2. Service (비즈니스 계층)
- **역할**: 비즈니스 로직을 처리하는 핵심 계층
- **책임**:
  - 트랜잭션 관리 (@Transactional)
  - 여러 Repository를 조합한 복잡한 비즈니스 로직
  - 인가 검사 (권한 확인)
  - 예외 처리

#### 3. Repository (데이터 접근 계층)
- **역할**: 데이터베이스와의 상호작용 담당
- **책임**:
  - JPA 쿼리 메서드 제공
  - CRUD 작업 수행
  - 데이터 조회 및 저장

---

## 핵심 개념

### 1. IoC (Inversion of Control) - 제어의 역전

**개념**: 객체의 생성과 관리를 Spring 컨테이너가 담당

**예시**:
```java
@Controller  // Spring이 이 클래스를 빈으로 등록
public class BoardController {
    private final BoardService boardService;  // Spring이 자동 주입
}
```

**장점**:
- 객체 간 결합도 감소
- 테스트 용이성 향상
- 유지보수성 향상

### 2. DI (Dependency Injection) - 의존성 주입

**개념**: 객체가 필요로 하는 의존성을 외부에서 주입받는 방식

**방식**: 생성자 주입 (권장)
```java
@RequiredArgsConstructor  // final 필드에 대한 생성자 자동 생성
public class BoardService {
    private final BoardRepository boardRepository;  // 생성자로 주입
}
```

**@RequiredArgsConstructor의 역할**:
- `final` 필드에 대한 생성자를 자동 생성
- `@Autowired` 어노테이션 없이도 의존성 주입 가능
- 불변 객체 보장 (final 사용)

### 3. @Transactional - 트랜잭션 관리

**개념**: 메서드 실행을 하나의 트랜잭션으로 묶어 일관성 보장

**특징**:
- 메서드 시작 시 트랜잭션 시작
- 정상 종료 시 자동 커밋
- 예외 발생 시 자동 롤백
- 더티 체킹(Dirty Checking) 활성화

**사용 예시**:
```java
@Transactional  // 읽기/쓰기 트랜잭션
public void 게시글수정(Long id, BoardRequest.UpdateDTO updateDTO) {
    Board board = boardRepository.findById(id).orElseThrow(...);
    board.update(updateDTO);  // 엔티티 변경
    // 트랜잭션 종료 시 자동으로 UPDATE 쿼리 실행 (더티 체킹)
}

@Transactional(readOnly = true)  // 읽기 전용 트랜잭션 (성능 최적화)
public List<Board> 게시글목록조회() {
    return boardRepository.findAllByOrderByCreatedAtDesc();
}
```

### 4. 더티 체킹 (Dirty Checking)

**개념**: JPA가 영속성 컨텍스트에 있는 엔티티의 변경을 자동 감지하여 UPDATE 쿼리 실행

**동작 방식**:
1. 엔티티를 조회하여 영속성 컨텍스트에 저장
2. 엔티티의 필드 값을 변경
3. 트랜잭션이 끝날 때 변경된 필드만 자동으로 UPDATE 쿼리 실행

**예시**:
```java
@Transactional
public void 게시글수정(Long id, BoardRequest.UpdateDTO updateDTO) {
    Board board = boardRepository.findById(id).orElseThrow(...);
    board.update(updateDTO);  // 필드 값 변경
    // save()를 호출하지 않아도 자동으로 UPDATE 쿼리 실행
}
```

**장점**:
- 개발자가 직접 UPDATE 쿼리를 작성할 필요 없음
- 변경된 필드만 업데이트 (성능 최적화)
- 코드 간결성 향상

### 5. Optional - null 안전성

**개념**: null 값을 안전하게 처리하기 위한 컨테이너 클래스

**사용 예시**:
```java
// 값이 없으면 예외 발생
Board board = boardRepository.findById(id)
    .orElseThrow(() -> new Exception404("게시글을 찾을 수 없습니다"));

// 값이 없으면 null 반환
User user = userRepository.findByUsername(username)
    .orElse(null);

// 값이 있는지 확인
if (userRepository.findByUsername(username).isPresent()) {
    throw new Exception400("이미 존재하는 사용자입니다");
}
```

### 6. 세션 기반 인증

**개념**: 서버에 사용자 정보를 세션에 저장하여 인증 상태를 유지

**동작 흐름**:
1. 로그인 성공 시 세션에 사용자 정보 저장
2. 다음 요청부터 세션에서 사용자 정보 확인
3. 로그아웃 시 세션 무효화

**예시**:
```java
// 로그인 시 세션에 저장
session.setAttribute("sessionUser", sessionUser);

// 세션에서 사용자 정보 조회
User sessionUser = (User) session.getAttribute("sessionUser");

// 로그아웃 시 세션 무효화
session.invalidate();
```

### 7. 인터셉터 (Interceptor)

**개념**: 컨트롤러 실행 전후에 공통 로직을 처리하는 컴포넌트

#### LoginInterceptor (인증 인터셉터)
- **역할**: 로그인하지 않은 사용자의 접근 차단
- **동작**: 컨트롤러 진입 전에 세션 확인
- **예외**: Exception401 발생

#### SessionInterceptor (세션 인터셉터)
- **역할**: 모든 뷰에 세션 사용자 정보 자동 주입
- **동작**: 컨트롤러 실행 후, 뷰 렌더링 전에 모델에 추가
- **효과**: 모든 Mustache 템플릿에서 `{{#sessionUser}}` 사용 가능

### 8. JPA 엔티티 관계

**@ManyToOne**: 다대일 관계
```java
@ManyToOne(fetch = FetchType.LAZY)  // 지연 로딩
@JoinColumn(name = "user_id")
private User user;
```

**LAZY 로딩**:
- 연관된 엔티티를 실제 사용할 때까지 조회하지 않음
- 성능 최적화 (필요한 경우에만 조회)
- N+1 문제 해결: `default_batch_fetch_size: 100` 설정

### 9. 쿼리 메서드 (Query Methods)

**개념**: 메서드 이름만으로 쿼리를 자동 생성

**예시**:
```java
// 생성일 기준 내림차순 정렬
List<Board> findAllByOrderByCreatedAtDesc();

// 사용자명으로 조회
Optional<User> findByUsername(String username);

// 사용자명과 비밀번호로 조회
Optional<User> findByUsernameAndPassword(String username, String password);
```

**네이밍 규칙**:
- `findAll`: 전체 조회
- `By`: 조건 시작
- `OrderBy`: 정렬 시작
- `Desc/Asc`: 내림차순/오름차순

### 10. DTO (Data Transfer Object)

**개념**: 계층 간 데이터 전송을 위한 객체

**사용 목적**:
- 엔티티와 요청/응답 데이터 분리
- 유효성 검사 로직 포함
- 엔티티 노출 방지

**예시**:
```java
public class BoardRequest {
    @Data
    public static class SaveDTO {
        private String title;
        private String content;
        
        public Board toEntity(User user) {
            return new Board(title, content, user);
        }
    }
    
    @Data
    public static class UpdateDTO {
        private String title;
        private String content;
        
        public void validate() {
            if(title == null || title.trim().isEmpty()) {
                throw new Exception400("제목은 필수입니다");
            }
        }
    }
}
```

---

## 주요 기능별 설명

### 1. 게시글 기능

#### 게시글 목록 조회
- **URL**: `GET /board/list` 또는 `GET /`
- **인증**: 불필요
- **기능**: 생성일 기준 내림차순 정렬

#### 게시글 상세 조회
- **URL**: `GET /board/{id}`
- **인증**: 불필요
- **기능**: 게시글 상세 정보 및 소유자 여부 확인

#### 게시글 작성
- **URL**: `POST /board/save`
- **인증**: 필요 (LoginInterceptor)
- **기능**: 로그인한 사용자가 게시글 작성

#### 게시글 수정
- **URL**: 
  - 화면: `GET /board/{id}/update`
  - 처리: `POST /board/{id}/update`
- **인증**: 필요
- **인가**: 게시글 소유자만 수정 가능
- **기능**: 더티 체킹을 활용한 수정

#### 게시글 삭제
- **URL**: `POST /board/{id}/delete`
- **인증**: 필요
- **인가**: 게시글 소유자만 삭제 가능

### 2. 사용자 기능

#### 회원가입
- **URL**: 
  - 화면: `GET /join`
  - 처리: `POST /join`
- **기능**: 
  - 사용자명 중복 체크
  - 유효성 검사 (이메일 형식 등)

#### 로그인
- **URL**: 
  - 화면: `GET /login`
  - 처리: `POST /login`
- **기능**: 
  - 사용자명과 비밀번호 확인
  - 세션에 사용자 정보 저장

#### 로그아웃
- **URL**: `GET /logout`
- **기능**: 세션 무효화

#### 회원정보 수정
- **URL**: 
  - 화면: `GET /user/update`
  - 처리: `POST /user/update`
- **인증**: 필요
- **인가**: 자기 자신의 정보만 수정 가능
- **기능**: 
  - 비밀번호 변경
  - 세션 정보 갱신

### 3. 인증/인가 처리

#### 인증 (Authentication)
- **의미**: "누구인가?" - 사용자 신원 확인
- **처리**: LoginInterceptor에서 세션 확인
- **예외**: Exception401 (인증 실패)

#### 인가 (Authorization)
- **의미**: "무엇을 할 수 있는가?" - 권한 확인
- **처리**: Service 레이어에서 `isOwner()` 메서드로 확인
- **예외**: Exception403 (권한 없음)

**예시**:
```java
// 인가 검사
if (!board.isOwner(userId)) {
    throw new Exception403("게시글 수정 권한이 없습니다");
}
```

---

## 설정 파일 설명

### application-dev.yml

#### 서버 설정
```yaml
server:
  servlet:
    encoding:
      charset: utf-8
      force: true
  port: 8080
```

#### 로그 설정
```yaml
logging:
  level:
    root: INFO           # 모든 라이브러리는 INFO 이상만 출력
    com.example: DEBUG   # 내 프로젝트는 DEBUG 이상 모두 출력
```

**로그 레벨 우선순위**: ERROR > WARN > INFO > DEBUG > TRACE

#### 데이터베이스 설정
```yaml
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MySQL  # H2를 MySQL 호환 모드로 실행
    username: sa
    password:
  
  h2:
    console:
      enabled: true  # localhost:8080/h2-console로 접속 가능
```

#### 초기 데이터 로딩
```yaml
spring:
  sql:
    init:
      mode: always
      data-locations:
        - classpath:db/data.sql
```

#### JPA 설정
```yaml
spring:
  jpa:
    hibernate:
      ddl-auto: create  # 애플리케이션 시작시 테이블 새로 생성
    show-sql: true      # SQL 쿼리 콘솔 출력
    properties:
      hibernate:
        format_sql: true                    # SQL 포맷팅
        default_batch_fetch_size: 100       # N+1 문제 해결
    defer-datasource-initialization: true   # data.sql을 Hibernate 초기화 이후 실행
```

**주요 설정 설명**:
- `ddl-auto: create`: 개발용 (기존 데이터 삭제됨)
- `show-sql: true`: 개발용 디버깅
- `default_batch_fetch_size: 100`: LAZY 로딩 시 배치로 한 번에 가져오기

---

## 주요 변경사항

### 1. 계층 구조 명확화
- **Controller**: HTTP 요청/응답 처리만 담당
- **Service**: 비즈니스 로직 및 트랜잭션 관리
- **Repository**: 데이터 접근만 담당

### 2. 의존성 주입 방식
- **변경 전**: `@Autowired` 필드 주입
- **변경 후**: 생성자 주입 (`@RequiredArgsConstructor` + `final` 필드)
- **이유**: 불변 객체 보장, 테스트 용이성 향상

### 3. 트랜잭션 관리
- **읽기 전용 트랜잭션**: `@Transactional(readOnly = true)` 적용
- **성능 최적화**: 조회 작업은 읽기 전용으로 설정

### 4. 더티 체킹 활용
- **변경 전**: 명시적으로 `save()` 호출
- **변경 후**: 엔티티 필드 변경만으로 자동 UPDATE
- **장점**: 코드 간결성, 성능 최적화

### 5. 인증/인가 분리
- **인증**: LoginInterceptor에서 처리 (세션 확인)
- **인가**: Service 레이어에서 처리 (`isOwner()` 메서드)
- **예외 처리**: Exception401 (인증), Exception403 (인가)

### 6. 예외 처리 중앙화
- **@ControllerAdvice**: 모든 컨트롤러의 예외를 한 곳에서 처리
- **커스텀 예외**: Exception400, Exception401, Exception403, Exception404, Exception500
- **일관된 에러 응답**: 각 예외별로 적절한 에러 페이지 반환

### 7. 인터셉터 활용
- **LoginInterceptor**: 인증이 필요한 URL 패턴에 적용
- **SessionInterceptor**: 모든 뷰에 세션 정보 자동 주입
- **URL 패턴 관리**: WebMvcConfig에서 중앙 관리

### 8. DTO 패턴 적용
- **정적 내부 클래스**: `BoardRequest.SaveDTO`, `BoardRequest.UpdateDTO`
- **유효성 검사**: DTO 내부에 `validate()` 메서드
- **엔티티 변환**: `toEntity()` 메서드로 DTO → Entity 변환

### 9. 엔티티 비즈니스 로직
- **도메인 로직**: 엔티티 내부에 비즈니스 메서드 추가
  - `isOwner()`: 소유자 확인
  - `update()`: 상태 변경
- **장점**: 응집도 향상, 재사용성 향상

### 10. Optional 활용
- **null 안전성**: Optional로 null 처리
- **명시적 처리**: `orElseThrow()`, `orElse()` 등으로 명확한 처리

---

## 핵심 학습 포인트

### 1. 계층별 책임 분리
- 각 계층은 자신의 책임만 수행
- Controller는 비즈니스 로직을 직접 처리하지 않음
- Service는 데이터 접근을 직접 하지 않음

### 2. 트랜잭션 관리
- Service 레이어에서 트랜잭션 관리
- 읽기 작업은 `readOnly = true` 설정
- 더티 체킹을 활용한 자동 UPDATE

### 3. 인증/인가 분리
- 인증: "누구인가?" (LoginInterceptor)
- 인가: "무엇을 할 수 있는가?" (Service 레이어)

### 4. 예외 처리
- 커스텀 예외로 명확한 에러 메시지
- @ControllerAdvice로 중앙 집중화
- HTTP 상태 코드와 일치하는 예외 클래스

### 5. Spring의 핵심 개념
- IoC: 객체 생명주기 관리
- DI: 의존성 주입 (생성자 주입 권장)
- AOP: 트랜잭션, 인터셉터 등

---

## 참고 사항

### 개발 환경
- **포트**: 8080
- **H2 콘솔**: http://localhost:8080/h2-console
- **데이터베이스**: 인메모리 H2 (애플리케이션 재시작 시 데이터 초기화)

### 주의사항
- YAML 파일은 2칸 공백 사용 (탭 사용 금지)
- 엔티티 필드 변경 시 애플리케이션 재시작 필요 (ddl-auto: create)
- 세션은 서버 메모리에 저장 (서버 재시작 시 세션 초기화)

---

**작성일**: 2026년
**프로젝트**: Spring Blog (SSR v1.1)

