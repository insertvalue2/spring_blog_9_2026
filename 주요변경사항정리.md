# 주요 변경사항 정리

## 9단계: 게시글 목록 페이징 처리

### 개요
게시글이 많아질 경우 한 번에 모든 게시글을 조회하는 것은 성능 문제를 야기할 수 있습니다.
책처럼 페이지를 나눠서 보여주는 페이징 기능을 구현했습니다.

---

## 📋 목차 (빠른 이동)

- [1. 페이징의 원리 (Database & Repository)](#1-페이징의-원리-database--repository)
    - [BoardRepository 변경사항](#boardrepository-변경사항)
- [2. 사용자 vs 개발자의 숫자 세기 (Controller)](#2-사용자-vs-개발자의-숫자-세기-controller)
    - [BoardController 변경사항](#boardcontroller-변경사항)
- [3. 스마트한 포장지 만들기 (DTO)](#3-스마트한-포장지-만들기-dto)
    - [BoardResponse.PageDTO 생성](#boardresponsepagedto-생성)
- [4. 화면에 뿌리기 (Mustache)](#4-화면에-뿌리기-mustache)
    - [list.mustache 변경사항](#listmustache-변경사항)
- [5. BoardService 변경사항](#5-boardservice-변경사항)
    - [방어적 코드 (Defensive Coding) - 입력값 유효성 검사](#방어적-코드-defensive-coding---입력값-유효성-검사)
- [📊 전체 흐름도 (Sequence Diagram)](#-전체-흐름도-sequence-diagram)
- [💡 학생들의 예상 질문 (Q&A)](#-학생들의-예상-질문-qa)
- [📝 칠판 판서용 요약](#-칠판-판서용-요약)
- [주요 파일 변경 목록](#주요-파일-변경-목록)
- [테스트 시나리오](#테스트-시나리오)

---

## 1. 페이징의 원리 (Database & Repository)

### 핵심 개념: LIMIT과 OFFSET

**"데이터가 100만 개라면 한 번에 다 가져올 수 있을까요? 책처럼 페이지를 나눠야 합니다."**

## Spring Data JPA로 페이징 구현하기

Spring Data JPA는 Pageable과 Page를 사용해 페이징을 간단히 구현 가능. 

### 2.1. 핵심 개념

- **Pageable**: 페이지 번호(몇 번째 페이지?), 페이지 크기(한 페이지에 몇 개?), 정렬(최신순?)을 설정.
    - 예: PageRequest.of(0, 3) → 1페이지에 3개 데이터.
    - **중요**: Spring은 페이지 번호를 0부터 세지만, 사용자에게는 1부터 보여줌.
- **Page**: 페이징된 데이터와 정보를 담음.
    - content: 현재 페이지의 데이터(게시글 목록).
    - totalPages: 전체 페이지 수.
    - hasPrevious/hasNext: 이전/다음 페이지가 있는지.
- **LIMIT과 OFFSET**
    
    **"데이터가 100만 개라면 한 번에 다 가져올 수 있을까요? 책처럼 페이지를 나눠야 합니다."**
    
    - **LIMIT**: 가져올 데이터 개수
    - **OFFSET**: 건너뛸 데이터 개수
    
    **예시**: 5개씩 보고 싶고, 2번째 페이지를 보고 싶다면
    
    - DB에게: "앞에서 5개 건너뛰고(OFFSET 5), 5개만 줘(LIMIT 5)"
    - SQL
    
    ```sql
     SELECT * FROM board_tb LIMIT 5 OFFSET 5
    ```
    
    - **LIMIT (개수):** "몇 개를 가져올래?" (1개, 2개, 5개...) -> **양(Quantity)**의 개념입니다.
    - **OFFSET (건너뛰기):** "앞에서 몇 개를 버릴래(건너뛸래)?" -> **0부터 시작**하는 개념이 맞습니다.


### BoardRepository 변경사항

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardRepository.java`

```java
/**
 * 게시글 페이징 조회 (작성자 정보 포함, JOIN FETCH 사용)
 * 
 * JOIN FETCH와 페이징을 함께 사용할 때 주의사항:
 * - JOIN FETCH를 사용하면 count 쿼리가 정확하지 않을 수 있음
 * - DISTINCT를 사용하여 중복 제거
 * - countQuery를 별도로 작성하여 정확한 개수 계산
 */
@Query(value = "SELECT DISTINCT b FROM Board b JOIN FETCH b.user ORDER BY b.createdAt DESC",
       countQuery = "SELECT COUNT(DISTINCT b) FROM Board b")
Page<Board> findAllWithUserOrderByCreatedAtDesc(Pageable pageable);
```

**코드 설명**:
- `Pageable pageable`: "몇 개 건너뛰고, 몇 개 가져와" 정보를 담은 객체
- `JOIN FETCH`: 게시글 가져올 때 작성자(User)도 같이 가져와! (N+1 문제 방지)
- `DISTINCT`: JOIN FETCH로 인한 중복 데이터 제거
- `countQuery`: "전체 페이지가 몇 쪽인지 알려면 전체 게시글 수가 필요하죠? JOIN FETCH가 있으면 개수 세는 게 꼬일 수 있어서, 개수 셀 때는 깔끔하게 Board만 세라고 따로 쿼리를 적어준 것입니다."

**생성되는 SQL**:
```sql
-- 데이터 조회 쿼리
SELECT DISTINCT b.*, u.* 
FROM board_tb b 
INNER JOIN user_tb u ON b.user_id = u.id 
ORDER BY b.created_at DESC
LIMIT 5 OFFSET 0

-- 개수 조회 쿼리 (countQuery)
SELECT COUNT(DISTINCT b) FROM board_tb b
```

---

## 2. 사용자 vs 개발자의 숫자 세기 (Controller)

### 핵심 개념: 인덱스 불일치 (Index Mismatch)

**"여러분은 숫자를 1부터 세나요, 0부터 세나요?"**

- **사용자**: "1페이지 보여줘!" (page=1)
- **프로그램(List)**: "네? 0번째 리스트 말씀이시죠?" (index=0)

### BoardController 변경사항

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardController.java`

```java
@GetMapping({"/board/list", "/"})
public String boardList(
        @RequestParam(defaultValue = "1") int page,  // 사용자는 1부터 시작
        @RequestParam(defaultValue = "5") int size,
        Model model) {
    // 페이지 번호 변환: 사용자는 1부터 시작하는 페이지 번호를 사용하지만,
    // Spring Data JPA의 Pageable은 0부터 시작하므로 1을 빼서 변환
    int pageIndex = Math.max(0, page - 1);
    
    BoardResponse.PageDTO boardPage = boardService.게시글목록조회(pageIndex, size);
    model.addAttribute("boardPage", boardPage);
    return "board/list";
}
```

**코드 설명**:
- `int pageIndex = Math.max(0, page - 1);`
    - 사용자가 `page=1`을 요청하면, DB에게는 `0번 페이지`를 달라고 해야 합니다.
    - 그래서 들어올 때는 `-1`을 해줍니다.
    - `Math.max(0, ...)`로 음수가 되지 않도록 보호합니다.

**변환 예시**:
- 사용자 요청: `/?page=1` → 내부 처리: `pageIndex = 0`
- 사용자 요청: `/?page=2` → 내부 처리: `pageIndex = 1`
- 사용자 요청: `/?page=3` → 내부 처리: `pageIndex = 2`

---

## 3. 스마트한 포장지 만들기 (DTO)

### 핵심 개념: 뷰 로직의 캡슐화

**"DB에서 가져온 날것의 데이터를 뷰(HTML)가 쓰기 편하게 가공해야 합니다."**

Mustache 같은 템플릿 엔진에서는 복잡한 계산(page + 1, total / size 등)을 하기 어렵습니다.
그래서 Java(DTO)에서 미리 다 계산해서 넘겨줘야 합니다.

### BoardResponse.PageDTO 생성

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardResponse.java`

```java
/**
 * 페이징 정보 DTO
 * 
 * Spring Data JPA의 Page 객체를 View에 전달하기 위한 DTO
 * 페이징 정보와 페이지 링크 정보를 포함합니다.
 */
@Data
public static class PageDTO {
    private List<ListDTO> content;          // 현재 페이지의 게시글 목록
    private int number;                      // 현재 페이지 번호 (0부터 시작)
    private int size;                        // 페이지 크기
    private int totalPages;                  // 전체 페이지 수
    private long totalElements;              // 전체 게시글 수
    private boolean first;                   // 첫 페이지 여부
    private boolean last;                    // 마지막 페이지 여부
    private boolean hasNext;                 // 다음 페이지 존재 여부
    private boolean hasPrevious;             // 이전 페이지 존재 여부
    private Integer previousPageNumber;      // 이전 페이지 번호 (없으면 null)
    private Integer nextPageNumber;          // 다음 페이지 번호 (없으면 null)
    private List<PageLink> pageLinks;       // 페이지 번호 링크 목록

    public PageDTO(Page<Board> page) {
        // 게시글 목록을 DTO로 변환
        this.content = page.getContent().stream()
                .map(ListDTO::new)
                .toList();
        
        // 페이징 정보 설정
        this.number = page.getNumber();
        this.size = page.getSize();
        this.totalPages = page.getTotalPages();
        this.totalElements = page.getTotalElements();
        this.first = page.isFirst();
        this.last = page.isLast();
        this.hasNext = page.hasNext();
        this.hasPrevious = page.hasPrevious();
        
        // 이전/다음 페이지 번호 설정 (1부터 시작하는 번호로 변환)
        this.previousPageNumber = page.hasPrevious() ? page.getNumber() : null;
        this.nextPageNumber = page.hasNext() ? page.getNumber() + 2 : null;
        
        // 페이지 링크 생성 (현재 페이지 기준 앞뒤 2페이지씩 표시)
        this.pageLinks = generatePageLinks(page);
    }

    /**
     * 페이지 링크 생성
     * 
     * 현재 페이지를 기준으로 앞뒤 2페이지씩 표시합니다.
     * 예: 현재 페이지가 5이면 [3, 4, 5, 6, 7] 표시
     */
    private List<PageLink> generatePageLinks(Page<Board> page) {
        List<PageLink> links = new ArrayList<>();
        
        int currentPage = page.getNumber() + 1;  // 0부터 시작하는 번호를 1부터 시작하는 번호로 변환
        int totalPages = page.getTotalPages();
        
        // 시작 페이지와 끝 페이지 계산
        int startPage = Math.max(1, currentPage - 2);
        int endPage = Math.min(totalPages, currentPage + 2);
        
        // 페이지 링크 생성
        for (int i = startPage; i <= endPage; i++) {
            PageLink link = new PageLink();
            link.setDisplayNumber(i);
            link.setActive(i == currentPage);
            links.add(link);
        }
        
        return links;
    }
}
```

**코드 설명**:
- `this.pageLinks = generatePageLinks(page);`
    - "구글이나 네이버 게시판 보면 아래에 [1] [2] [3] [4] [5] 이렇게 숫자 나오죠? 그 숫자를 만드는 로직입니다."
    - 로직의 핵심: 현재 페이지가 5페이지라면, 앞뒤로 2개씩 (3, 4, **5**, 6, 7) 보여주도록 계산합니다.

- `previousPageNumber`, `nextPageNumber`
    - "HTML에서 링크 걸기 편하게, 아예 이동할 페이지 번호를 여기서 계산해서 줍니다. 뷰는 그냥 출력만 하면 되게요."

**페이지 링크 생성 예시**:
- 전체 페이지: 10페이지
- 현재 페이지: 5페이지
- 생성되는 링크: [3, 4, **5**, 6, 7] (5가 active)

---

## 4. 화면에 뿌리기 (Mustache)

### 핵심 개념: DTO 활용

**"DTO가 밥상을 다 차려놨으니, 숟가락만 얹으면 됩니다."**

### list.mustache 변경사항

**파일**: `src/main/resources/templates/board/list.mustache`

```mustache
{{#boardPage.content}}
    <div class="card mb-3">
        <div class="card-body">
            <h4 class="card-title mb-3">{{title}}</h4>
            <p class="text-muted">
                작성자: {{username}} | 작성일: {{createdAt}}
            </p>
            <a href="/board/{{id}}" class="btn btn-primary">상세보기</a>
        </div>
    </div>
{{/boardPage.content}}

{{^boardPage.content}}
    <div class="text-center mt-5">
        <h5>등록된 게시글이 없습니다</h5>
        <p class="text-muted">첫 번째 게시글을 작성해보세요!</p>
        <a href="/board/save" class="btn btn-primary">새글쓰기</a>
    </div>
{{/boardPage.content}}

<!-- 페이지 네비게이션 -->
<ul class="pagination d-flex justify-content-center">
    <!-- 이전 페이지 버튼 -->
    {{#boardPage.previousPageNumber}}
        <li class="page-item">
            <a class="page-link" href="/?page={{boardPage.previousPageNumber}}&size={{boardPage.size}}">Previous</a>
        </li>
    {{/boardPage.previousPageNumber}}
    {{^boardPage.previousPageNumber}}
        <li class="page-item disabled">
            <a class="page-link" href="#">Previous</a>
        </li>
    {{/boardPage.previousPageNumber}}

    <!-- 페이지 번호 -->
    {{#boardPage.pageLinks}}
        <li class="page-item {{#active}}active{{/active}}">
            <a class="page-link" href="/?page={{displayNumber}}&size={{boardPage.size}}">{{displayNumber}}</a>
        </li>
    {{/boardPage.pageLinks}}

    <!-- 다음 페이지 버튼 -->
    {{#boardPage.nextPageNumber}}
        <li class="page-item">
            <a class="page-link" href="/?page={{boardPage.nextPageNumber}}&size={{boardPage.size}}">Next</a>
        </li>
    {{/boardPage.nextPageNumber}}
    {{^boardPage.nextPageNumber}}
        <li class="page-item disabled">
            <a class="page-link" href="#">Next</a>
        </li>
    {{/boardPage.nextPageNumber}}
</ul>
```

**코드 설명**:
- `{{#boardPage.pageLinks}}`: DTO가 만들어준 숫자 리스트를 반복만 하면 됩니다.
- `{{#active}}active{{/active}}`: "현재 내가 보고 있는 페이지는 색깔을 다르게 칠해줘!" (Bootstrap 클래스 활용).

---

## 5. BoardService 변경사항

**파일**: `src/main/java/org/example/demo_ssr_v1_1/board/BoardService.java`

```java
/**
 * 게시글 목록 조회 (페이징)
 * 
 * @param page 페이지 번호 (0부터 시작, 기본값: 0)
 * @param size 페이지 크기 (기본값: 5)
 * @return 페이징된 게시글 목록 DTO
 */
@Transactional(readOnly = true)
public BoardResponse.PageDTO 게시글목록조회(int page, int size) {
    // Pageable 생성 (페이지 번호, 페이지 크기, 정렬 기준)
    int validPage = Math.max(0, page);
    int validSize = Math.max(1, Math.min(50, size));
    
    // 정렬 기준: 생성일 기준 내림차순 (최신순)
    Sort sort = Sort.by(Sort.Direction.DESC, "createdAt");
    Pageable pageable = PageRequest.of(validPage, validSize, sort);
    
    // JOIN FETCH로 Board와 User를 한 번의 쿼리로 함께 조회 (페이징 적용)
    Page<Board> boardPage = boardRepository.findAllWithUserOrderByCreatedAtDesc(pageable);
    
    // 트랜잭션 내에서 Page 객체를 PageDTO로 변환
    return new BoardResponse.PageDTO(boardPage);
}
```

**주요 변경사항**:
- 반환 타입: `List<BoardResponse.ListDTO>` → `BoardResponse.PageDTO`
- `Pageable` 객체 생성 및 페이징 처리
- 페이지 크기 제한: 최소 1개, 최대 50개

### 방어적 코드 (Defensive Coding) - 입력값 유효성 검사

이 코드는 **"방어적 코드(Defensive Coding)"** 또는 **"입력값 유효성 검사(Input Validation)"**라고 부릅니다.

사용자(프론트엔드)가 악의적으로 이상한 값을 보내거나, 실수로 잘못된 값을 보냈을 때 서버가 에러를 뱉거나 뻗어버리는 것을 방지하기 위해 **"강제로 안전한 값으로 보정"**하는 로직입니다.

#### 1. 페이지 번호 방어 (validPage)

```java
int validPage = Math.max(0, page);
```

**목적**: 페이지 번호가 음수가 되는 것을 막습니다.

**작동 원리**: `Math.max(A, B)`는 A와 B 중 더 큰 숫자를 선택합니다.

**시나리오**:
- **정상**: 사용자가 0을 보냄 → `Math.max(0, 0)` → 0 (통과)
- **에러/해킹**: 사용자가 실수로 -1이나 -5를 보냄 → `Math.max(0, -5)` → 0 (0이 -5보다 크므로 0으로 강제 변환)

**이유**: Spring Data JPA의 `PageRequest`에 음수를 넣으면 에러가 발생합니다.

#### 2. 페이지 크기(Size) 방어 (validSize)

이 부분이 조금 복잡해 보이지만, **"최소값과 최대값 사이로 숫자를 가두는(Clamping)"** 아주 유명한 패턴입니다.

```java
int validSize = Math.max(1, Math.min(50, size));
```

이 코드는 **"아무리 작아도 1, 아무리 커도 50"**으로 만들겠다는 뜻입니다. 안쪽부터 해석하면 쉽습니다.

##### 1단계: 최대값 제한 (Math.min) - "상한선"

`Math.min(50, size)`: 둘 중 더 작은 것을 고릅니다.

**시나리오**:
- 사용자가 1000개 달라고 요청함 (`size = 1000`)
- `Math.min(50, 1000)` → 50 선택

**이유**: 누군가 `size=1000000`으로 요청하면 DB 데이터를 100만 개 다 긁어오다가 서버 메모리가 터집니다(OOM - Out Of Memory). 그래서 **"최대 50개까지만 줄 거야"**라고 막는 것입니다.

##### 2단계: 최소값 제한 (Math.max) - "하한선"

`Math.max(1, 결과값)`: 둘 중 더 큰 것을 고릅니다.

**시나리오**:
- 사용자가 0개나 -10개 달라고 요청함 (`size = 0` or `-10`)
- (1단계에서 `Math.min(50, 0)`은 0이 됨)
- `Math.max(1, 0)` → 1 선택

**이유**: 0개를 조회하는 건 쿼리 낭비이고, 음수 개수는 말이 안 되므로 최소 1개는 조회하도록 강제합니다.

#### 👨‍🏫 학생들을 위한 한 줄 요약 설명

**"여러분, 만약 사용자가 주소창에 `?page=-5&size=999999`라고 입력하면 어떻게 될까요? 서버가 다운될 수도 있겠죠? 이 코드는 사용자가 어떤 엉뚱한 값을 보내더라도, **'페이지는 0번 이상, 사이즈는 1~50개 사이'**라는 안전한 범위 내로 값을 강제로 고쳐서 서버를 보호하는 안전장치입니다."**

이 코드가 없으면 서버는 매우 취약해지므로 실무에서는 필수적인 패턴입니다.

**실무 예시**:
- **공격 시나리오**: 악의적인 사용자가 `?page=-999999&size=999999999` 요청
- **방어 코드 없이**: 서버 에러 발생 또는 메모리 부족으로 서버 다운
- **방어 코드 있으면**: `validPage = 0`, `validSize = 50`으로 안전하게 처리

---

## 📊 전체 흐름도 (Sequence Diagram)

```
sequenceDiagram
    participant User as 사용자(Browser)
    participant Controller as 컨트롤러
    participant Service as 서비스
    participant DB as 데이터베이스

    User->>Controller: GET /board/list?page=1 (1페이지 줘!)
    Note over Controller: 사용자의 1을 <br/>컴퓨터의 0으로 변환<br/>(page - 1)
    Controller->>Service: 게시글목록조회(page=0)
    Service->>DB: LIMIT 5 OFFSET 0 (0번부터 5개 줘)
    DB-->>Service: 데이터 5개 + 전체 개수 반환 (Page 객체)
    Note over Service: DTO 변환 작업<br/>1. Entity -> DTO<br/>2. 페이지 번호 계산 (0 -> 1)<br/>3. [1][2][3] 링크 생성
    Service-->>Controller: PageDTO 반환
    Controller-->>User: HTML 응답 (사용자는 1페이지를 봄)
```

---

## 💡 학생들의 예상 질문 (Q&A)

### Q1: Repository에 DISTINCT는 왜 썼나요?

**A**: "우리가 JOIN FETCH를 쓰면 DB 내부적으로 데이터가 뻥튀기(중복)될 가능성이 있습니다. 자바 객체로 만들 때 중복을 제거하고 깔끔하게 가져오기 위해 붙여주는 안전장치입니다."

**예시**:
- JOIN FETCH 없이: Board 5개 → 5개 반환
- JOIN FETCH 사용 시: Board 5개 + User 5개 → 중복 가능성
- DISTINCT 사용: 중복 제거하여 정확히 5개 반환

### Q2: countQuery 안 쓰면 에러 나나요?

**A**: "JOIN FETCH가 단순하면 괜찮을 수도 있지만, 복잡해지면 하이버네이트가 '어? 이거 개수 어떻게 세야 하지?' 하고 에러를 뱉거나, 메모리에 모든 데이터를 다 로딩해서 직접 세려고 시도합니다(메모리 폭발). 그래서 '개수는 그냥 Board 테이블만 세면 돼!'라고 알려주는 게 성능상 필수입니다."

**성능 비교**:
- countQuery 없이: 전체 데이터를 메모리에 로딩 후 개수 계산 (느림, 메모리 낭비)
- countQuery 사용: COUNT 쿼리만 실행 (빠름, 메모리 효율적)

### Q3: 왜 사용자는 1부터, 프로그램은 0부터 세나요?

**A**: "사용자 입장에서는 자연스럽게 1페이지, 2페이지라고 생각하지만, 프로그래밍에서는 배열이나 리스트의 인덱스가 0부터 시작하는 것이 일반적입니다. 그래서 Controller에서 변환해주는 것입니다."

**변환 로직**:
```java
// 사용자: page=1 (1페이지)
// 내부: pageIndex=0 (0번째 페이지)
int pageIndex = page - 1;
```

### Q4: 페이지 링크를 왜 DTO에서 만들나요?

**A**: "Mustache 같은 템플릿 엔진에서는 복잡한 계산을 하기 어렵습니다. Java에서 미리 계산해서 넘겨주면, 템플릿은 그냥 반복문으로 출력만 하면 됩니다. 이렇게 하면 뷰 로직이 단순해지고 유지보수가 쉬워집니다."

**장점**:
- 뷰 로직 단순화
- 비즈니스 로직과 뷰 로직 분리
- 테스트 용이성 향상

---

## 📝 칠판 판서용 요약

### 페이징 처리 4단계

1. **Repository**: LIMIT/OFFSET으로 데이터 가져오기
    - `Pageable` 객체 사용
    - JOIN FETCH + DISTINCT + countQuery

2. **Controller**: 사용자 번호(1부터) → 프로그램 번호(0부터) 변환
    - `int pageIndex = page - 1;`

3. **DTO**: 뷰에서 쓸 데이터 미리 계산
    - 페이지 링크 생성
    - 이전/다음 페이지 번호 계산

4. **View**: DTO가 준 데이터 그대로 출력
    - 반복문으로 페이지 번호 표시
    - active 클래스로 현재 페이지 강조

---

## 주요 파일 변경 목록

1. `BoardRepository.java`: 페이징 쿼리 메서드 추가
2. `BoardService.java`: 페이징 메서드 추가
3. `BoardController.java`: 페이징 파라미터 처리
4. `BoardResponse.java`: PageDTO 클래스 추가
5. `list.mustache`: 페이징 UI 추가

---

## 테스트 시나리오

1. **기본 페이징**: `/?page=1&size=5` - 첫 페이지 5개 조회
2. **다음 페이지**: `/?page=2&size=5` - 두 번째 페이지 5개 조회
3. **페이지 링크**: 현재 페이지 기준 앞뒤 2페이지씩 표시 확인
4. **이전/다음 버튼**: 첫 페이지에서는 이전 버튼 비활성화, 마지막 페이지에서는 다음 버튼 비활성화
5. **게시글 없음**: 게시글이 없을 때 안내 메시지 표시 확인

